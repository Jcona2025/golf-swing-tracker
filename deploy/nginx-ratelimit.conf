# Drop in /etc/nginx/conf.d/swinglogger-ratelimit.conf
# Defines a 1MB shared-memory zone keyed by client IP, capped at ~5 req/min.
# Used by the location = /upload block in the swinglogger vhost.
limit_req_zone $binary_remote_addr zone=swinglogger_upload:1m rate=5r/m;
limit_req_status 429;
